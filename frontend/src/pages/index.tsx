import { Container } from '@mui/material';
import Header from '../components/layout/Header';

const Home = () => {
  return (
    <Container>
      <Header />
    </Container>
  );
};

export default Home;
